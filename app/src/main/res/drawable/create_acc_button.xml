<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/pretty_black" /> <!-- Background color -->
    <stroke
        android:width="2dp"
        android:color="@color/pretty_blue" />
    <corners android:radius="15dp" /> <!-- Corner radius -->
</shape>
